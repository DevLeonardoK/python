[project]
name = "python"
version = "0.1.0"
description = "A Python learning repository to track my progress and experiments."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "memory-profiler>=0.61.0",
]
